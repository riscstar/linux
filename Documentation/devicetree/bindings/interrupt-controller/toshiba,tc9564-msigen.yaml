# SPDX-License-Identifier: (GPL-2.0-only OR BSD-2-Clause)
%YAML 1.2
---
$id: http://devicetree.org/schemas/interrupt-controller/toshiba,tc9564-msigen.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#

title: Toshiba TC9564 MSIGEN Interrupt Controller

maintainers:
  - Daniel Thompson <daniel@riscstar.com>
  - Alex Elder <elder@riscstar.com>

description: |
  The Toshiba TC9564 (a.k.a Qualcomm QPS615) is an SoC accessed by a
  host system through the upstream PCIe port on the PCIe switch it
  implements.  The switch includes an embedded PCIe endpoint that
  provides access to various SoC peripherals (including an interrupt
  controller) via its BARs.

  The TC9564 MSIGEN (MSI Generator) provides an interrupt controller
  with 25 hardware interrupt lines.  It aggregates internal interrupts
  and generates a single PCIe MSI to the host.  Each PCIe function
  implements an MSIGEN instance.

  Each MSIGEN uses a gated clock and reset line internal to the TC9564,
  provided by its clock and reset controllers.  The clock and reset
  lines for both functions are accessed by a PCI endpoint bus provided
  by the first PCIe function.

allOf:
  - $ref: /schemas/interrupt-controller.yaml#

properties:
  compatible:
    const: toshiba,tc9564-msigen

  reg:
    maxItems: 1

  interrupt-controller: true

  '#interrupt-cells':
    const: 1

  clocks:
    maxItems: 1
    description: Gate clock from the TC9564 clock controller

  resets:
    maxItems: 1
    description: Reset signal from the TC9564 reset controller

required:
  - compatible
  - reg
  - interrupt-controller
  - '#interrupt-cells'

additionalProperties: false

examples:
  - |
    #include <dt-bindings/clock/toshiba,tc9564.h>
    #include <dt-bindings/reset/toshiba,tc9564.h>

    interrupt-controller@f000 {
        compatible = "toshiba,tc9564-msigen";
        reg = <0xf000 0x100>;
        interrupt-controller;
        #interrupt-cells = <1>;
        clocks = <&tc9564_clock0 CLOCK_MSIGEN>;
        resets = <&tc9564_reset0 RESET_MSIGEN>;
    };
